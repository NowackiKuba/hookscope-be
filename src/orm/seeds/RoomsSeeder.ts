import { EntityManager } from '@mikro-orm/core';
import { Seeder } from '@mikro-orm/seeder';

/** Stub: Rooms module not yet implemented. Run when rooms entity is available. */
export class RoomsSeeder extends Seeder {
  async run(_em: EntityManager): Promise<void> {
    // No-op until rooms module exists
  }
}
