import { EntityManager } from '@mikro-orm/core';
import { Seeder } from '@mikro-orm/seeder';

/** Stub: Reservations module not yet implemented. Run when reservations entity is available. */
export class ReservationsSeeder extends Seeder {
  async run(_em: EntityManager): Promise<void> {
    // No-op until reservations module exists
  }
}
